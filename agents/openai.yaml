interface:
  display_name: "Writing Loop Editor / 写作 Loop 编辑器"
  short_description: "Bilingual long-form writing and revision"
  default_prompt: "Use $writing-loop-editor to rewrite this passage with reader-agent diagnosis first. / 使用 $writing-loop-editor 先做读者复述诊断，再改写这段文字。"

policy:
  allow_implicit_invocation: true
