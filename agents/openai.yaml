interface:
  display_name: "Writing Loop Editor"
  short_description: "Clear long-form writing and revision"
  default_prompt: "Use $writing-loop-editor to rewrite this passage with reader-agent diagnosis first."

policy:
  allow_implicit_invocation: true
