interface:
  display_name: "Rossum Universal Writer / 罗素姆万能写手"
  short_description: "Rossum universal writing and revision"
  default_prompt: "Use $rossum-universal-writer to rewrite this passage with reader-agent diagnosis first. / 使用 $rossum-universal-writer 先做读者复述诊断，再改写这段文字。"

policy:
  allow_implicit_invocation: true
